/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package com.icrypt.app;

import com.icrypt.list.LinkedList;

import static com.icrypt.utilities.StringUtils.join;
import static com.icrypt.utilities.StringUtils.split;
import static com.icrypt.app.MessageUtils.getMessage;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        System.out.println(join(tokens));
    }
}
